package org.example.stageback.repository.parametrage.marine;

import org.example.stageback.model.parametrage.marine.OperationMarine;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OperationMarineRepository extends JpaRepository<OperationMarine,Long> {
}
